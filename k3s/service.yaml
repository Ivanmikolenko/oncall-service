apiVersion: v1
kind: Service
metadata:
  name: oncall-service
  namespace: ${NAMESPACE}
  labels:
    app: oncall-service
spec:
  type: NodePort
  selector:
    app: oncall-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: ${NODE_PORT}
